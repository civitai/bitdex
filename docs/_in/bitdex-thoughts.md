Well, there's your answer. So here's how you do it. You...we don't need to process all kinds of things at the same time. In the same way that we figured out that you can do a single pass through a batch, through something that's been gathered. So what you do is instead you gather a single CSV into batches. So you can be essentially processing tags really...well, you can't process tags until the very end of the day. but at the beginning you can process all the little things. So there's something to do kind of all the way through. And then as time goes by, then you move on to the bigger things. And actually, I'm realizing that you actually need to do the reverse. You should be working on the bigger thing first, burning that down, and then when you don't have anything left, then you start to take smaller and smaller ones. And we know the size of these files so we can do that. So we spend the majority of the time doing using tags. And then once we have tags in memory, you know, we put them into the bitmap and we put them onto disk. Do we put them onto disk? I think you can. I think you can. So I'll explain that in just a second. So you put them onto disk. It doesn't really matter the order. They just need to have their image ID and their data. Because at the header of the document, you have the image IDs in the thing. It a cached thing You can just rapid read through it It doesn matter the order They don actually have to be grouped like an object because you can read the whole thing and then it an object in your memory So anyways as soon as you process a full CSV then working happens Essentially we working to burn that out of memory So to do that we prepared the bitmap that basically as we preparing the bitmap we putting stuff into the doc stores They not compressed yet but they are message So then you move on to the next biggest thing but by doing it this way You've offloaded all of the memory of the tag CSV. You don't need to touch any of that anymore. It's all on disk. Everything can be released except like the overhead, you know, the Rion stuff. But you're very conservative about what you carry. So you do your best to not clone things if you don't need to, and to make sure that you're using references as much as you can. So then, by doing this, you basically are just burning down the size of the stuff that you have to have in memory, and things are much smaller in memory, as bitmaps, for example. So it makes it easier to do that transition, and then by taking little chunks at a time and compressing them, you create this compressive that's the best document with indexes that you can change at any time, and that's infinitely cacheable. So this thing, the CSV, is kind of like a core. It's a document that you can pull into a bitdex, and NDJSON is the same thing. But I think CSVs are great because it's just data. And here we are organizing it. We're saying, hey, every everything's attached to an image. This is an image index. This is an image bitdex.


---

If it doesn't work with the doc store format, it needs to. This is the doc store format. So, if that's not how doc store format is working right now, that's how it needs to be. Because the way that it works is we pull these bitmaps into memory, and it helps us know which of these documents matter. So we can say, hey, look at this file. Okay, give me all of the fives stuff, and all of that. and all of the seven stuff, and it's a single scan, and all of a sudden you have everything that you need for all of those documents.

And because disk is so fast, it's negligible. It's less than one millisecond to get anything from any of these caches at the same time. 880,000 per second based on our benchmarks from these caches with the document. So in under a millisecond, it's a massive unlock. So we just need to make it so that it's easy to get stuff into them, and that's this process. This is the loader. And loaders have essentially two sides. They have, you know, the doc store and the bitdex. That's the bitmaps that are essentially the index for the store. And then they have, on the other side, things that are providing tuples, essentially. there, the ID of the thing that goes into the store, and a field name, and a value, and that's it. And so you're putting things into the doc store. They have an ID, they have the what it is, and they have its value. And its value can be... you define the type of these things with a configuration. And you can define this type just as you load things. And AI can actually create the types for these things. So the whole thing is designed to be essentially an AI document store.

I've even talked with you guys about embedding a micro-LLN, like a function Gemma, which you should look up. I think that there's probably open source versions now that we can just run locally that are super fast. And I think you can even put in the browser, which would be just wild. And if you can't, here we go. We're gonna make one. that's not the same. But anyway, this little micro-LLM can essentially be the router on top of these things. You can just give it a semantic query, and it will find the right thing based on the filters, and it will add indexes on top of your documents based on direction that it receives from bigger LLMs. So the bigger LLMs can be like, oh, I want these types of things, and the Bitdex agent will say, hey, here are the filters. and other AI features then the LLMs will essentially try and utilize the Doc store and that will create caches and it will also teach the LLM oh if they asking for this type of thing give them this thing So you essentially training these things as part of the process And because Bitdex is designed to be essentially a cache it makes it so it instant and you can chain them together And they respond so fast fast, like nanosecond speed, that it makes an intelligent network of data. And data isn't just information. It's not static, necessarily. It can be components. It can be models. It can be functions, right? Like software. Really, if you think about it, and what Bitdex is is just a sort of software protocol. And it's interesting because I keep stumbling into these as I have time to fully explore things with AI, and it's been so cool. I love it.

and it's so fun to finally be creating this thing that I've been like thinking about and dreaming about for, I don't know, almost a year now. But I think that this is the beginning of the thing that AI's, that these agents store all of their data in. Because this makes the perfect memory. because it can just be whatever they want. Because it's not just CSVs that you'll load, you also put data into it. So if you have the ability to make routers on top of data, you can put a router on top of routers. So you can have function gemas that are designed to direct traffic amongst data and actually link them together. And again, it's just another bit dex. And so you get essentially views, instantly materialized and cached views of data.

But the coolest part is the data is not just static. It can grow and adapt, and it's instant. You don't have to think about joins anymore. It's just gone. And I think, you know, I've been fighting to keep things as fast as possible. If it takes longer than a millisecond, then I have room to improve. because the dream is to make it so that you can combine all of these things to essentially have unlimited connective access to data of any kind that can be fully driven and discovered and maintained by an AI agent. And it's not any slower than... I don't know. I mean, it's not any slower than... I don't know. responding in under a millisecond from a network of data that can do anything. That's crazy.

So to resummarize, loaders are things that just ingest tuples. We kind of need a new word, a new word for this, because it's not a tuple, it's three things. It's a bit header. It's, you know, in this case an image ID. A field and a value. Yeah. So a bit header, a bit header, a field, and a value. So what would you call that? A bit field? A bit tuple? A tuple bit? I don't know. Need to come up with a name there. But because they can live within these bound caches, everything's super fast. And ingest. Ingest is about just putting in these bittuples as fast as you can because there's essentially workers that are just working as fast as they can to ingest the content that you give them. So they're taking the bit header, they're putting it into the bitmap, they map the field, and they map the value, where the slot of that combination is the bit is the value of the bit So when you pass in something that like ID9 then you turning on the ninth bit on any tuple So you just turned up flipped the bit header for that tuple And bitmaps aren necessarily flat They can have layers And I think that that needs a concept or a name too I think that like a bit stack That what it is So it a stack of bitmaps So our sort layers are basically that So think about it like a sieve where to find in the same way. So, when you're using a different set of values, you look through the Civ for the values that align with what you want. And you need to start thinking about numbers a little bit differently because what you realize is that with 32 layers, you can represent all kinds of numbers by turning on and off different elements in that bit stack, different bits and layers. And so you can represent all kinds of numbers by turning on and off different elements in that bit stack, different bits and layers. and so you can represent data between zero and I think it's two billion for the cost of just how full your sieve is. And it's not even that. It's like if it's more than 50% empty or 50% empty, that's 50% full, it's efficient. The more close you are to either end, the better. It means that it costs less. But what that does is it makes it so you can sort anything, you know, with the Civ, and you can filter by anything with the tuple maps. and find you just layer to find what you want you just stick them all on top of each other You start smallest to biggest And sometimes requests are nested and so you resolve in kind of like a hierarchical order Sometimes no it got to be top essentially So you would find the smallest on the top layer and used that to stack on top. And then you'd look at the smallest of the next layer and you'd stack the smallest on top. So you're trying to shrink the set as much as you can, as quickly as you can, because you're just operating by putting all of these layers into CPU memory. You can do that. That's what they're designed for. They do millions of those per second. And so it gives you this kind of unlimited capacity to find that you can find anything quickly. It's crazy because it completely breaks the way that search indexes and databases and all of that work. They just don't really make any sense once you have the ability to do this. So I think about Postgres, for example. I don't know why I would use it anymore. As long as I can make it so that this thing is durable, then we good It can stand on its own And it will be so much faster than Postgres And it hardly takes any resources That the craziest part And it auto I mean that insane You just choose the values that you want to have filterable and sortable and you set And because of this bitmap thing I can also construct a vector bitmap And that can be layers So when you representing you know a vector space you just stack up 512 layers or something to represent a hash space And you can probably do that in multiple directions So you can probably make 3D stacks or something So you can represent multiple axes using bitmap and really it just needs to be smaller than the size of an L3, and you're good. You're sub-microsecond at that point. You're nanoseconds. So vector search, they're just doing it wrong. Because you get to your, you get into an area, you get into your cache, in the search group. And then you run the semantic search on top of that. You do the cosine similarity on top of that. You find the most efficient way to find the distance between what they want and what's there. And it's funny because the answer is actually something that you use higher up the stack. Just a function that was created to do that. You have function Gemma routing you to data and then down to the stack. and further you have a function that's been created to do that routing, to do that management, I guess. Anyways, this is going really meta, but I hope you can take this and help me turn this into a document that really explains what it is that we're building, because I feel like it's been something I've been uncovering and I feel like it's something that needs to be understood by anybody working on this project.

So, essentially, bitmaps just serve the purpose of creating different layers of caches. And I think that's the way that we think about it, is you have these little mini caches that start at 4KB, and they grow over time with a support system that helps keep them tight and fast. And then you have the big daddies, the bitmaps that live behind the scenes that provide the data. So, cache is the answer, the database is the bitmaps, or the index is the bitmaps, or the bitdex is the bitmaps. And then the documents that are represented by that data you can fetch at any time. So in the same way that you can choose setup indexes in Postgres, with this you just choose the things you want to be able to sort by and the things you want to be able to filter by. The end. and you can actually define those indexes. We'll have to come up with the syntax for defining indexes. I think that there's work that we need to do on the kind of schema definition side of things. We need to put together a sort of DDL for the platform. I like the idea of it being visible, being config-driven, so just being a JSON and then being able to just reload that. I think that, you know, conceptually, for me, it's easy to see. I think that I almost like it better represented by a YAML style. style thing or maybe even something like Prisma style thing. I'll have to show you kind of the syntax that I like, but it's almost like a variant of YAML. It kind of looks like this:

```
images
    id? 1 // number (default is selected as needed - schemas evolve, u8->u16->u32->u64)
    user*? // filterable
    type? ''[] // low-cardinality-string - backed by a bitdictionary
    tags*[]? | tag*

user
    id? 1
    name?~ ''
    email? ''
    images*[] // relationships appear on both sides and indexes are prepared on demand - we have automated index creation (1M records in 500ms)
```

Whoa! If I can create an index on anything in seconds! I don't need to think about it in advance. They can be made on demand and stored then auto-cached.
Data automatically creates live caches:
- If we've seen it once we have the answer
- Hot (memory): 12-16μs
- Warm (disk shard restore): ~337μs
- Cold (full filter + sort): 40-70ms broad, 1.6ms sparse

```
* relationship (stored as id)
? filterable
'' string
1 number
[] array/multivalue
```


bitdict - backed by a bitmap so that they can easily be used in ops
key 1
value ''

Okay, so I want to think a little bit about these bit dictionaries. I'm trying to think about the data storage for them. You know, you need to be able to jump to a value really easily here, and vice versa. But you don't want to store, I mean, I guess they have dash maps, right? Isn't that what this would be?

is a dash map essentially similar to a bitmap where you don't have to keep a whole array. I just have this thing in my mind of like a roaring bitmap with... how it could give direction to data, to a value. Because, I mean, that's exactly what we're doing here with Bitdex. But this is like at the lowest level. And it's like, because you can compress things, you can represent values with for free, basically, with minimal cost by paying attention to on and off. And you can count through slots, through these bits, and with that, assign value to a thing. You can provide an index to a document, to a whatever. So these are essentially the tuples They can be the tuples The bit tuples Weird Am I recognizing that this thing is so fast because it can represent because it a because it is just the bitmap. The concept of a compressed bitmap is what makes this thing possible, and I'm having just such a hard time grokking it, but it means... And such a fast disk. You can access anything rapidly just by creating maps to it. and choosing maps that are efficient, well-calculated, so that you can compress things further. Or not. If they're not, you know, well, if it's half and half, then it costs you more. But if it's anything different than that, you're saving. so compression. It's just another level of compression. So you're compressing the keys and you're compressing the data with DocStore. And then bound caches are the caches that make it so you can not have to consider the whole universe, just little windows.

---

So, the way that ASR should work is it should be streaming as soon as you click the button. And then when you stop the button, then you have your audio. And better yet, you know, you do it like some of these other things. It shows you what you're saying as you say it, and then you let go, and then it's in. I think I could make that pretty easily actually, especially with Civitai's ASR system. If I can get it to be performant, which I think that I can with this Bitdex thing, then I can build this really easily and I would need that as a product. So anyways, I think that cypherate needs to be a thing. I'm wondering if I don't do it with anybody on my team. Doesn't seem like any of them are going to be a good thing. that I'm interested. I should just do it on my own. It's a good chance for me to do my own thing. The investors essentially turned it down. My team's so consumed with Civitai. I think it's my project to go do Cypherate. Super cool.